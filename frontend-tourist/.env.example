# Base URL of the backend API. Read at build time by Vite.
# Local dev falls back to http://localhost:4000 if unset.
# Production (Render): https://atoll-isle-api.onrender.com
VITE_API_BASE=http://localhost:4000
